<!-- this will show all the creatures -->
<h3>It's Dino Time!</h3>
<h4>Dino party</h4>
<ul>
    <% dinos.forEach(dino => { %>
    <li>
        <p><strong><%= dino.name %></strong> is a 
        <%= dino.type %></p>
    </li>
    <% }) %>
</ul>